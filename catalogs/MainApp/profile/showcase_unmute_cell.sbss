import showcase_following_cell.sbss

if "${muted}" != "yes" || "${is-me}" == "yes"
    #btn_action: enabled=no
    if "${is-me}" == "yes"
        #btn_action: label="본인은 언뮤트할 수 없습니다."
    else
        #btn_action: label="뮤트한 사용자가 아닙니다."    
    end
end
