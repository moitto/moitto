import showcase_following_cell.sbss

if "${follows}" == "yes" || "${is-me}" == "yes"
    #btn_action: enabled=no
    if "${is-me}" == "yes"
        #btn_action: label="본인은 팔로우할 수 없습니다."
    else
        #btn_action: label="팔로우 중입니다."    
    end
end
