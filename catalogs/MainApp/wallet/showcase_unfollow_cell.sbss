import showcase_following_cell.sbss

if "${follows}" != "yes" || "${is-me}" == "yes"
    #btn_action: disabled=yes
    if "${is-me}" == "yes"
        #btn_action: label="본인은 언팔로우할 수 없습니다."
    else
        #btn_action: label="팔로우한 사용자가 아닙니다."    
    end
end
